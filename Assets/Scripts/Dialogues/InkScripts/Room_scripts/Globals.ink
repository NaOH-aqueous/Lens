VAR read_times_mirror = 0

VAR check_box_times = 0

VAR curtain_open = false //if the curtain has been opened

VAR door_open = false //if the door has been unlocked

VAR have_paper = false //if the player has taken the paper

VAR lamp_switch = true //if the switch has been turned on

VAR read_notes = false

VAR window_clean = false